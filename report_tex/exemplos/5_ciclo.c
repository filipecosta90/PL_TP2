// declaracoes inciais
int a;
int b;
int c;
int maior;

// operacoes de atribuicao
a = 1;
a = b; // estamos a atribuir directamente a b o valor de a
c = 20 % 1;

// instrucoes ciclicas
do {
  do {
    a = a + 1;
  }
  while ( a < c )
  b = b + 1;
}
while ( b < c )
